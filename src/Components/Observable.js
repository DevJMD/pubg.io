import {
    Observable,
    Subject,
    ReplaySubject,
    from,
    of,
    range
} from 'rxjs';

import {
    map,
    filter,
    switchMap
} from 'rxjs/operators';

export const RXJS = {
    Observable,
    Subject,
    ReplaySubject,
    from,
    of,
    range,
    map,
    filter,
    switchMap
}